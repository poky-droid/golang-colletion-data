name: Test Runner

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Test Runner
        run: |
          echo "RUNNER BERHASIL"
          whoami
          hostname
          pwd