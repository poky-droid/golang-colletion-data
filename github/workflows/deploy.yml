name: Test Self Hosted Runner

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Test Runner
        run: |
          echo "Runner berhasil!"
          whoami
          hostname
          pwd
          uname -a